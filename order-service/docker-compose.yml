services:
  postgres:
    image: postgres:15
    container_name: ordersys-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ordersys
    ports:
      - "5432:5432"